# Copy this file to .env for local `netlify dev` testing, and set the
# same variable in the Netlify dashboard for your deployed site under:
# Site configuration -> Environment variables
#
# Optional. If unset, the admin login falls back to the public default
# passcode "admin123" until you change it from Admin -> Account.
ADMIN_DEFAULT_PASSCODE=change-me-before-launch
